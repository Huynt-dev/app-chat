import styled from "styled-components";
import { Button, Input } from "antd";

export const InputMail = styled(Input)`
  display: inline-block;
  width: calc(70% - 8px);
  margin-right: 8px;
`;

export const ButtonMail = styled(Button)`
  display: inline-block;
  width: calc(30%);
`;

export const ButtonApplyPass = styled(Button)`
  margin-right: 8px;
`;
